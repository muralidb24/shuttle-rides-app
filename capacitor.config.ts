import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.postalcolony.shuttlerides',
  appName: 'Neighborhood Shuttle Rides',
  webDir: 'dist'
}

export default config
